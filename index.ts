// index.js (na raiz do projeto)

import { registerRootComponent } from 'expo';
import App from './src/App'; // Importa o seu componente principal da pasta 'src'

// registar o componente App como o componente raiz da aplicação
registerRootComponent(App);
